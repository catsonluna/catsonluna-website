import styles from "@/styles/Tags.module.css";


export function RustTag() {
  return (
    <div className={`${styles.tag} ${styles.rust}`}>
      Rust
    </div>
  );
}

export function TypeScriptTag() {
  return (
    <div className={`${styles.tag} ${styles.typescript}`}>
      TypeScript
    </div>
  );
}

export function PythonTag() {
  return (
    <div className={`${styles.tag} ${styles.python}`}>
      Python
    </div>
  );
}

export function JavaTag() {
  return (
    <div className={`${styles.tag} ${styles.java}`}>
      Java
    </div>
  );
}

export function NextJsTag() {
  return (
    <div className={`${styles.tag}  ${styles.nextjs}`}>
      Next.js
    </div>
  );
}

export function ReactNativeTag() {
  return (
    <div className={`${styles.tag} ${styles.react}`}>
      React Native
    </div>
  );
}

export function GoTag() {
  return (
    <div className={`${styles.tag} ${styles.go}`}>
      Go
    </div>
  );
}

export function DiscontinuedTag() {
  return (
    <div className={`${styles.tag} ${styles.discontinued}`}>
      Discontinued
    </div>
  );
}

export function TauriTag() {
  return (
    <div className={`${styles.tag} ${styles.tauri}`}>
      Tauri
    </div>
  );
}